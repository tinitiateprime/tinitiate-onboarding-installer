# Optional classroom overrides. Every Compose file works without a .env file.
CODE_SERVER_PASSWORD=Tinitiate!23456

# Host ports can be changed when the defaults are already in use.
CODE_SERVER_PORT=8080
FLOCI_UI_PORT=4500
FLOCI_AWS_PORT=4566
FLOCI_AZURE_PORT=4577
FLOCI_GCP_PORT=4588

# Base64 for "local-floci-student-key". This is local test data, not a cloud key.
FLOCI_AZURE_ACCOUNT_KEY=bG9jYWwtZmxvY2ktc3R1ZGVudC1rZXk=

# Defaults used only by the matching cloud environment.
AWS_DEFAULT_REGION=us-east-1
GCP_PROJECT_ID=floci-local

# Optional on-premises database overrides. Its YAML works without these values.
DATABASE_TIMEZONE=America/Chicago
MSSQL_PORT=1433
MSSQL_SA_PASSWORD=change-me-Mssql1!
POSTGRES_PORT=5432
POSTGRES_USER=tinitiate
POSTGRES_PASSWORD=change-me-postgres
POSTGRES_DB=tinitiate
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=change-me-MysqlRoot1!
MYSQL_DATABASE=tinitiate
MYSQL_USER=tinitiate
MYSQL_PASSWORD=change-me-mysql

# Oracle Developer appliance.
ORACLE_PORT=1521
ORACLE_ADMIN_PASSWORD=change-me-OracleAdmin1!
ORACLE_APP_USER=tinitiate
ORACLE_APP_PASSWORD=change-me-OracleApp1!

# AI appliance database, vector database, and MinIO data lake.
AI_POSTGRES_PORT=5432
AI_POSTGRES_USER=tinitiate
AI_POSTGRES_PASSWORD=change-me-ai-postgres
AI_POSTGRES_DB=ai_lab
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=tinitiate
MINIO_ROOT_PASSWORD=change-me-minio

# Provider credentials are optional until an exercise calls a real AI service.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AZURE_AI_PROJECT_ENDPOINT=
AZURE_AI_MODEL_DEPLOYMENT_NAME=
ANTHROPIC_API_KEY=
CUSTOM_AI_BASE_URL=http://host.docker.internal:11434/v1
CUSTOM_AI_API_KEY=
CUSTOM_AI_MODEL=

# dbt local PostgreSQL target.
DBT_POSTGRES_PORT=5432
DBT_POSTGRES_USER=tinitiate
DBT_POSTGRES_PASSWORD=change-me-dbt-postgres
DBT_POSTGRES_DB=analytics
