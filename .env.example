# Copy this file to .env and change both passwords.
CODE_SERVER_PASSWORD=change-me-code
POSTGRES_DB=student_dev
POSTGRES_USER=student
POSTGRES_PASSWORD=change-me-database

# Host ports can be changed when the defaults are already in use.
CODE_SERVER_PORT=8080
POSTGRES_PORT=5432
